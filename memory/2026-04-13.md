# MEMORY - 2026-04-13

## 🎯 Mục tiêu hôm nay
- Chuẩn hóa toàn bộ hệ thống ảnh sản phẩm (Catalog Imagery standardization).
- Reset STT Folder và Ảnh Main theo thứ tự Brand (A-Z).
- Tạo folder trống cho toàn bộ 271 sản phẩm.

## ✅ Kết quả đạt được
- **Dọn dẹp:** Xóa hết folder rác, merge 13 folder trùng lặp, 6 folder orphan.
- **Rename SEO:** Gần 1,100 file ảnh con được đổi tên chuẩn SEO.
- **STT Rename:** Tận dụng 84 folder có ảnh cũ để đánh số STT, đồng thời tạo mới 187 folder trống cho các sp còn lại.
- **Main Images:** Đã đánh số STT cho ~260 ảnh main.
- **Sync Code:** Update `mockData.ts` (1,580 dòng) và `searchIndex.ts` (92KB) trỏ về cấu trúc STT mới.
- **Git:** Đã push toàn bộ thay đổi lên `main` (`ba73f32`).

## 🚧 Công việc dở dang
- Di chuyển file lẻ (nuoc-hoa--thuc-te, tom-ford-real) vào folder STT tương ứng.
- Hoàn thiện script convert tự động (JPG -> WebP) khi User upload ảnh mới.
- Fix nốt ~10 ảnh main chưa tự nhận diện được STT.

## 💡 Bài học / Ghi chú
- Windows hay bị lỗi `EBUSY` khi `renameSync` do Explorer đang mở folder đó. Giải pháp: Dùng `copyFileSync` + `unlinkSync` (Hoặc dùng Temp dir).
- Khi update `mockData.ts` số lượng lớn, nên dùng line-by-line processing để tránh làm hỏng cấu trúc file.
