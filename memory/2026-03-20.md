## 2026-03-20 12:39 — OpenClaw reinstall / reset context
- User gửi screenshot xác nhận kế hoạch reset máy để giảm lag và làm sạch process.
- Nội dung plan đã lưu ở artifact `openclaw_reinstall_plan.md` và có backup credentials tại `D:/anti/ai-team/CREDENTIALS_BACKUP.md`.
- Sau khi reset, việc cần làm theo plan:
  1. Xóa 4 scheduled tasks cũ gây conflict.
  2. Xóa `.openclaw` cũ + uninstall OpenClaw.
  3. Cài lại mới + config sạch 1 file duy nhất.
  4. Test Telegram.
  5. Tạo 1 scheduled task duy nhất thay cho 4 task xung đột.
- Đây là context cần nhớ để hỗ trợ user tiếp tục sau reset, tránh hỏi lại từ đầu.
