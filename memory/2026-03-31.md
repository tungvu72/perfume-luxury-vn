- Người dùng chốt chiến lược media cho Maison de SON: ảnh main chuẩn vẫn là packshot/catalog; ảnh thực tế tự chụp là gallery bổ sung để tăng trust; video tạm thời tách khỏi scope hiện tại và sẽ làm sau.
- Đã tạo SOP chính cho pipeline sản phẩm + ảnh main + ảnh thực tế tại `docs/ops/real-image-product-sop.md`; file này được chốt là nguồn quy trình duy nhất cho phạm vi này, và yêu cầu deprecated các quy trình cũ liên quan.
- Người dùng chốt hướng watermark: áp dụng watermark cho cả ảnh main lẫn ảnh thực tế; rule chi tiết còn để Anti review trong SOP nhưng đây là định hướng đã khóa.
- Trang production `/nuoc-hoa-theo-nhu-cau` đã được Anti xử lý xong lỗi ảnh nền đen/mất ảnh bằng batch chuyển ảnh về JPEG thật; production render ảnh đã ổn hơn, nhưng còn vòng UI polish riêng cho card/grid.
- Khi làm việc với Anti, người dùng muốn quy trình rất chặt: phải check trực tiếp trên web (ưu tiên production; khi đánh giá UI phải xem cả desktop và mobile) trước khi phản hồi; không được dựa vào báo cáo suông.
- Intake ảnh thực tế từ thư mục `D:\anti\ảnh nước hoa` đang ở giai đoạn refine script matching (`match_intake.js` -> `refine_intake.js` -> `refine_intake_v2.js`); chưa được phép sang publish/attach ảnh hoặc tạo sản phẩm mới cho đến khi qua thêm một vòng cleanup logic cuối.
- Các guardrail người dùng muốn giữ rất chặt cho pipeline ảnh thực tế: không dump toàn bộ ảnh lên web, không tự tạo item mới chỉ vì có folder ảnh, không gắn ảnh khi match chưa chắc, và ảnh 10ml không được làm người xem hiểu nhầm thành sản phẩm chính.
