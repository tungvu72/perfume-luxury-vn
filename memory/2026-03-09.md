## 00:14 - Durable memory flush
- CEO rejected product-page image handling when wrong perfume/version images were shown; new hard rule established: if product image is not confidently verified as the correct bottle/version, block it from live and use placeholder/verification state instead of guessing.
- Implemented product image verification system: placeholder image `public/placeholders/product-image-pending.svg`, `verified?: boolean` on gallery images, UI badge for unverified images, and all 8 current seed products were moved to safe blocked/pending image state.
- Product copy strategy changed significantly based on CEO feedback: write like a mainstream Vietnamese perfume user, not like AI or over-technical reviewer. Avoid terms like `DNA`, `verdict`, and overblown perfumery jargon in user-facing copy.
- Product content process updated in `PRODUCT_CONTENT_PROCESS_v2.md`: every product detail must include 4 internal anchors (home `/`, category page, brand page, self product page) plus info-rich sections (origin, release year, story, flankers/versions, smell impression, when to use, Maison de SON opinion).
- Rewrote all 8 seed product records in `src/constants/mockData.ts` with more human Vietnamese tone and info-rich product detail sections.
- CEO strongly disliked English-facing labels/tags and raw HTML-looking article output on product pages; fixed by cleaning product article rendering in `src/app/product/[id]/ProductClient.tsx`, auto-linking at render layer, and Vietnamizing visible labels/tags.
- UI/content direction reinforced: product article body should look softer and easier to read; avoid robotic summary blocks and stiff top-of-page remarks.
- Important operational lesson: do not inject `<a>` HTML directly into product article strings; keep article plain text/markdown-like and insert links safely in render logic.
- Current repo state after latest fixes: product article rendering and Vietnamese labels cleaned in commit `cdf92a7` (`fix: clean product article rendering and vietnamese labels`).
- New overnight execution target from CEO: expand from the initial 20 planned additions to **50 hot products total** for morning review. Scope is text/data/SEO/product-page completion only; images are explicitly deferred for Antigravity, so all new products can stay in placeholder/pending-image safe-state.
- CEO approved future memory/retrieval infrastructure for Maison de SON. Decision: create backlog item for **Qdrant setup** after current product-foundation sprint. Timing rule: do not install now; revisit after product/data/image baseline is stable. Intended use later: semantic retrieval across products, articles, rules, and workspace memory.

## 21:15 Pre-compaction memory flush
- User rejected long-form product detail layout as too text-heavy and explicitly wants mobile product pages to learn from / closely mimic Fragrantica's compact, data-first mobile flow.
- Key UX decisions from user:
  - Remove duplicate lower-page Kenperfume reference price block; keep primary price near the top only.
  - Product page should surface core info within ~1-2 scrolls: hero, quick facts, accords, notes, performance, versions; review goes later.
  - Avoid tall score blocks, long vertical note timelines, large whitespace, and prose-heavy sections.
- Working direction locked in: Fragrantica-style mobile clone/wireframe documented in `PRODUCT_PAGE_MOBILE_WIREFRAME.md`.
- Caution learned today: bulk patching UTF-8 Vietnamese content through shell caused mojibake multiple times; prefer minimal/safe edits and validate encoding carefully before claiming done.
- Current implementation status before compaction:
  - `src/app/product/[id]/ProductClient.tsx` was rolled back to HEAD to recover from broken encoding.
  - Kenperfume lower price block was removed again after reset work.
  - Next concrete task is to code one clean sample mobile layout for `bleu-chanel-edp` following the Fragrantica-style wireframe.
