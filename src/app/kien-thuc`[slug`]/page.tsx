import fs from 'fs'; export async function generateStaticParams() { const files = require('fs').readdirSync('content/buying-guides').filter((f:string) => f.endsWith('.md')).map((f:string) => ({ slug: f.replace('.md', '') })); return files; } export default async function ArticlePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const content = require('fs').readFileSync('content/buying-guides/' + slug + '.md', 'utf-8'); const title = content.split('\n')[0].replace(/^#\s*/, ''); return (<article className='max-w-4xl mx-auto px-4 py-8'><a href='/kien-thuc' className='text-blue-600'>Back</a><h1 className='text-3xl font-bold my-4'>{title}</h1><pre className='whitespace-pre-wrap text-sm bg-gray-50 p-4'>{content}</pre></article>); }